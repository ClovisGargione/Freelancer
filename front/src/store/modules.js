import { store as auth } from '@/modules/auth'
import { store as search } from '@/modules/search'
import { store as worker } from '@/modules/worker'
import { store as profession } from '@/modules/profession'
import { store as activity } from '@/modules/activity'
import { store as rating } from '@/modules/rating'

export default {
  auth,
  search,
  worker,
  profession,
  activity,
  rating
}
