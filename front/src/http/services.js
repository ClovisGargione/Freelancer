import { services as auth } from '@/modules/auth'
import { services as search } from '@/modules/search'
import { services as worker } from '@/modules/worker'
import { services as profession } from '@/modules/profession'
import { services as activity } from '@/modules/activity'
import { services as rating } from '@/modules/rating'

export default {
  auth,
  search,
  worker,
  profession,
  activity,
  rating
}
